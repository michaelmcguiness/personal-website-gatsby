import React from 'react';

import { HomeWrapper, P } from './Styling';

const HomePage = (props) => {
    return (
        <HomeWrapper>
            <P>Today's essay:</P>
        </HomeWrapper>
    )
}

export default HomePage;